{% extends "@UmbrellaAdmin/layout.html.twig" %}

{% set current_route = app.request.attributes.get('_route') %}

{% block content %}
    {{ nav_config({'active_strategy': 'current_route'}, 'pills') }}
    {{ nav_start() }}
        {{ nav_item({'label' : 'How to', 'route': 'app_admin_menu_howto'}) }}
        {{ nav_item({'label' : 'Exemple', 'route': 'app_admin_menu_exemple'}) }}
    {{ nav_end() }}

    <div class="tab-content">
        <div class="tab-pane active">
            {% block tab_content %}
            {% endblock %}
        </div>
    </div>
{% endblock %}