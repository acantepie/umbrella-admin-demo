{{ nav_config('bordered') }}
{{ nav_start() }}
{{ nav_item({'label' : 'Preview', 'url': '#preview-' ~ id}) }}
{{ nav_item({'label' : 'Code', 'url': '#code-' ~ id}) }}
{{ nav_end() }}

<div class="tab-content">
    <div class="tab-pane active" id="preview-{{ id }}">
        {% block form %}{% endblock %}
    </div>
    <div class="tab-pane" id="code-{{ id }}">
        {% block code %}{% endblock %}
    </div>
</div>