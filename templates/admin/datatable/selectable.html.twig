{% extends "@UmbrellaAdmin/layout.html.twig" %}

{% block content %}
    <div class="card">
        <div class="card-body pb-0">
            <h4 class="card-title">Selectable example</h4>
            <p class="text-muted font-14">
                Example of Datatable with custom select plugin.<br>
            </p>
        </div>
        <div class="card-datatable">
            {{ render_table(table) }}
        </div>
    </div>
{% endblock %}
