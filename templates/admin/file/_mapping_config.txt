umbrella_core:
  file:
    mappings:
      custom.mapping:
        # Vous devez définir un controller qui servira vos fichiers sur cet url
        uri: /foo/bar/{storageName}/{name}

        # Le fichier sera stocké dans le répertoire /baz de votre flysystem
        # Si cette valeur n'est pas définit, il utilisera l'id (dans cet exemple : /custom)
        path_prefix: /baz