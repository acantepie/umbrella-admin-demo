class MyMenu
{
    public function createMenu(MenuBuilder $builder) : Menu
    {
        $builder
            ->root()
            ->add('foo')
                ->add('bar')
                    ->setLabel('bar label')
                    ->end()
                ->add('baz')
         ...

        return $builder->getMenu();
    }
}