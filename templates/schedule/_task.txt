class MyTask extends AbstractTask
{

    /**
     * {@inheritdoc}
     */
    public function execute($env)
    {
        if (isset($env['say']) {
            echo $env['say'];
        }

        ...

    }
}
